use :

    python buildadd.py --directory .

to scan all directories and create thumbnails and a contents.html in all of them.

use 

    python remove_thumbnails.py


ro remove all thumb*png recursively
